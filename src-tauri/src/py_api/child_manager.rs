#[derive(Debug)]
pub struct ChildManager {}

impl ChildManager {
    #[allow(dead_code)]
    pub fn new() -> Self {
        ChildManager {}
    }
}

impl Default for ChildManager {
    fn default() -> Self {
        ChildManager {}
    }
}
